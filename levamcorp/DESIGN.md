---
name: Levam Corp Distributors
description: B2B wholesale distributor site — warehouse/shipping visual world
colors:
  bg: "#000000"
  bg-panel: "#1D1A15"
  bg-panel-elevated: "rgba(29,26,21,0.6)"
  brand-blue: "#2F7DF6"
  kraft: "#B98A54"
  kraft-deep: "#8F6636"
  steel: "#6B7280"
  success-green: "#12B76A"
  text-primary: "#F5F1E8"
  text-muted: "#A7A090"
  border: "rgba(245,241,232,0.07)"
typography:
  display:
    fontFamily: "Space Grotesk, -apple-system, sans-serif"
    letterSpacing: "-0.02em"
  body:
    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, Helvetica Neue, Arial, sans-serif"
  mono:
    fontFamily: "SF Mono, JetBrains Mono, ui-monospace, Menlo, monospace"
rounded:
  sm: "4px"
  md: "6-10px"
  lg: "12px"
components:
  button-primary:
    backgroundColor: "linear-gradient(135deg, {colors.brand-blue}, #0284C7)"
    textColor: "#ffffff"
    rounded: "{rounded.sm}"
    padding: "12px 24px"
  button-ghost:
    backgroundColor: "rgba(255,255,255,0.03)"
    textColor: "#ffffff"
    rounded: "{rounded.sm}"
    padding: "12px 24px"
  card:
    backgroundColor: "linear-gradient(160deg, rgba(accent,0.16) 0%, rgba(accent,0.03) 45%, {colors.bg-panel} 80%)"
    rounded: "10px"
---

## Overview

Levam Corp is a B2B wholesale electronics distributor (Doral, FL) selling to resellers — chiefly Amazon/Walmart sellers. The public marketing site (`src/app/page.js` and its nav-linked pages) runs a **warehouse / shipping** visual world: the site should feel like the inside of a real distribution operation, not a generic dark-mode SaaS dashboard. This world was established in this redesign pass; **the admin panel still runs an older "mission control" dark-blue tech theme** (`#080B14` bg, pure blue glow orbs) that has not been migrated — do not assume site-wide consistency there until that migration happens. **The client portal (`src/app/portal/**`) has been fully migrated to this same visual vocabulary — but on a white/light ground, not the homepage's black.** All seven pages (sign-in, dashboard, catalog, orders, invoices, payments, reset-password) are done, migrated one at a time on explicit request; see the per-page notes below for what each pass covered and any anti-patterns caught along the way. **Portal ground is `#FFFFFF`, not `#000000`** — explicit request, since this is an everyday-use working app (not a marketing page) and a light background reads as more familiar for that. **One documented exception: the sign-in page (`src/app/portal/page.js`) runs on the dark `#08090B` ground with an animated backdrop, per a later, dedicated Claude Design handoff for that page specifically** — see its entry below; every page behind the gate still follows the white-ground rule. The scan-ticket vocabulary (mono header strips, cream `#F2EFE6` tickets, dark `#08090B` ink-stamp panels, brand-blue `#2F7DF6` accent, barcode rulers) carries over unchanged; only the base tone flips. Cream ticket panels that read fine floating on the homepage's black ground need a thin `border:1px solid rgba(8,9,11,0.1)` on portal pages to stay visually distinct from the now-white page — without it they visually merge into the page. Dark `#08090B` panels (nav bar, account-record cards) need no such border; they already contrast against white on their own.

## Colors

Committed-strategy palette: one saturated accent — **Levam's own brand blue, `#2F7DF6`** — carries CTAs, dividers, ticket accents and links sitewide (the logo's "CORP" and every prior brand-blue use already used this exact color; the accent was folded into it rather than kept as a second color, specifically so accent and logo always match). Kraft-cardboard tones still carry material surfaces, steel-gray is still the secondary neutral. Ground is **true black (`#000000`)** — deliberately not a tinted charcoal — so cards, photos and tickets read as objects floating in void rather than sitting in a tinted room. Text/borders use warm off-white and warm gray, not cool blue-gray.

**This page's neutral tone and accent color each changed twice over this redesign pass — both by explicit request, in this order:**
1. Ground started as warm charcoal-brown (`#14120E`). Changed to true black (`#000000`) — sitewide, every literal `#14120E` and `rgba(20,18,14,…)` in `src/app/page.js` (nav, hero scrims, mobile-menu overlay, the vignette, card/panel backgrounds) — because the brown read as a tinted room rather than the void the "floating object" look needs.
2. Accent started as each mockup's own default orange (`#ff7a45`) — the first eight Claude Design handoffs (About, Language/Insights, Catalog/Founders, the sitewide backdrop, FAQ) each had that swapped for a documented "committed" safety-yellow (`#F2B705`), reasoning that yellow was already this doc's accent. On the ninth handoff (Contact/Footer) that swap was reversed on request — the accent became the mockups' own `#FF7A45` orange, ported as-is instead of overridden. Immediately after, on the tenth request, the orange itself was replaced site-wide with Levam's brand blue (`#2F7DF6`), because orange didn't match the blue logo. **Do not introduce a third accent color without being asked — port a future mockup's own accent only if the user says to; otherwise it should be `#2F7DF6`.**

Do not reintroduce `#080B14` / cool blue-gray text (`#9AACC9`, `#F0F4FF`) on this page — that's the legacy system this world replaced.

Sections do **not** carry their own ambient radial-gradient "glow blob" (a 350–500px soft circle in some accent color, positioned in a corner). An earlier pass had ~17 of these scattered one or two per section, rotating through blue/yellow/green/steel — removed on explicit feedback that it read as scattered colored dots rather than one cohesive background. The ground is flat warm charcoal plus the fixed steel-grid texture layer (behind everything, set once in the page root); color now lives in the Card tags, the video, and the hazard-strip, not in floating per-section spotlights. The two remaining full-bleed `radial-gradient(ellipse ...)` washes (hero, final CTA) are a different, much subtler device — under 0.10 opacity, spanning the whole section rather than reading as a discrete shape — and are fine to keep or reuse sparingly; the discrete circular blob is what's banned.

## Typography

Space Grotesk (display/headlines) + Inter (body) are a **confirmed cross-site asset** — already shipped on 7+ pages (admin, portal, login) before this redesign — kept here for brand consistency rather than picked fresh for this surface. `.lc-mono` (SF Mono/JetBrains Mono) is reserved for manifest/tracking-number-style data (the hero badge, the stamp captions, the brand marquee label) — it represents actual shipping/inventory data, not a "technical" costume.

## Shapes

Corners stay small and functional (4–12px) — this is an operational/industrial world, not a soft consumer one. The CSS `preserve-3d` shipping-box treatment (open box, flaps folding to sealed, driven by scroll) was removed from the hero — the video needed the full frame, uncompeted by a foreground object — and wasn't kept elsewhere; a future surface can reintroduce that technique deliberately, but there's no `.box3d-*` scaffolding left to resurrect verbatim.

## Components

- **HeroVideoBackground** (the hero, `src/app/page.js`): a full-bleed video background that autoplays through 8 real clips of a shipment's actual journey (label → carton → pallet → forklift → trailer → distribution centre → delivered), looping the set forever. **Deliberately not scroll-linked.** An earlier version scroll-scrubbed the sequence inside a `1000vh` sticky stage; that shipped broken twice — first `crossOrigin="anonymous"` made the browser hard-fail every clip fetch (no matching CORS headers from the CDN), then even after that fix the visitor still hit a long dead scroll distance whenever a clip stalled, because the whole page's scroll was hijacked by the video's timeline. This component has no loader, no gate, and nothing can block on it: the hero's text renders immediately regardless of video state, a failed clip just advances to the next one, and the page always scrolls at normal 1:1 speed. Do not re-couple this to scroll position without a much stronger reason than "it looked cool in the mockup" — two production incidents came from that coupling. **Two stacked `<video>` elements crossfade** (`JOURNEY_FADE_SEC = 0.7`s): the idle element is preloaded with the next clip well ahead of time and starts playing (both elements briefly in motion together) via a `timeupdate` check firing at `duration - JOURNEY_FADE_SEC`, so the cut dissolves while both clips still have real motion in them — not a hard swap on `ended`, which read as a jarring pop/freeze between clips. `ended` is kept only as a safety-net trigger in case `timeupdate` doesn't fire in time.
- **Hero overlay content** (`src/app/page.js`, inside the same `100vh` hero section as `HeroVideoBackground` — rebuilt from a second Claude Design handoff): the badge/headline/CTA/stats block that sits on top of the video. The headline no longer types itself out character-by-character (`TypewriterText` is gone); each of its three lines now reveals with a masked slide-up (`heroLine` keyframe, `overflow:hidden` wrapper + inner `translateY(105%)→0`), matching the imported design instead of a typewriter effect. A thin safety-yellow underline wipes in below it (`heroWipe`). The badge gained a live `LiveClock` (`America/New_York`, ticks every 15s) inline with "B2B WHOLESALE · DORAL FL". A new mono category-tag row ("APPLIANCES / AUDIO / TV & DISPLAY / GAMING") sits between the subcopy and the CTAs. The stats block changed from a 4-column bordered grid to a single dot-separated row (`HeroStats`) that counts up from 0 on mount via an eased `requestAnimationFrame` (cubic ease-out, 1.4s) — same real numbers as before (48h dispatch, 500+ SKUs, 100% B2B), the standalone "7+ premium brands" stat was dropped since the brand marquee now carries that signal visually. A "SCROLL" cue with an animated vertical line sits at the bottom-right of the content row (`hero-scroll-cue`, hidden under 900px). The whole content block tilts a few degrees toward the cursor (`HeroTiltGroup`, tracks `mousemove` on the hero `<section>`, imperative `rAF`-throttled transform — same pattern as `TiltCard`, just without its glow/scale).
- **BrandMarquee** (`src/app/page.js`): replaced the old text-only `BrandTicker` (generic all-caps brand names scrolling behind diamond bullets). Pinned to the very bottom edge of the hero section (`position:absolute; bottom:0`), overlapping the last strip of video behind a blurred dark scrim, not a separate section further down the page. Scrolls real logo images (`/public/brands/*.png` — SharkNinja, JBL, Logitech, Harman Kardon, DJI, Anker, Amazon, Hisense, Samsung, KitchenAid, Nintendo, nutribullet, PlayStation; already light-on-transparent, no CSS invert filter needed) at `opacity:0.72`, brightening to `1` + a slight scale on hover (`.brand-logo-item:hover`). Reuses the existing `ticker` keyframe (list tripled, `-33.333%` translate) rather than adding a new one.
- **Card** (shared `Card` component — every info block on the site: categories, "why partners choose us", company info, founders, insights, contact): reads as a **manifest tag pulled off a crate**, not a generic bordered card. Dashed (not solid) accent-tinted border, a clipped hang-tag notch on the top-right corner (`clip-path`), a grommet hole top-left, and a header row above the content with a deterministic `TAG·XXXX` code (last 4 hex digits of the accent color) plus a tiny inline barcode (`Barcode` sub-component, bar widths derived from the same code) separated from the body by a perforated rule. Accent-tinted gradient wash from the accent color to `{colors.bg-panel}` is unchanged underneath. Accent rotates across sections between brand-blue, safety-yellow, success-green, kraft, and steel — never purple/gold (retired earlier). Do not revert this to a plain solid-border rounded rectangle — that was explicitly called out as reading like every other site's generic card component.
- **StampSeal was removed** — its only two call sites (the "Florida Registered" / "Verified Partners" badges in About) were replaced by `SealRing` (see `CompanyRecord` below) when that section was rebuilt from a Claude Design mockup; it had no other callers, so the ink-stamp "thud" bounce-easing pattern is gone from the codebase. If a future surface wants a badge that reveals on scroll, that's a deliberate pattern to reach for again, but there's no component left to resurrect verbatim.
- **hazard-strip**: a diagonal yellow/charcoal repeating-gradient divider (warehouse caution-tape motif), used sparingly as a section break, not on every seam.
- **CategoryLabel** (categories section only, `src/app/page.js`): imported verbatim from a user-provided Claude Design mockup — a literal printed shipping label, not a themed dark card. Cream label stock (`#f4f2ec` background, `#111` ink, `#dcd8ce` border) with a deep shadow, deliberately breaking from the site's dark ground for this one section — the "real label pinned to a dark wall" contrast is intentional, not a bug to fix. Structure, top to bottom: a `#000000` brand header bar (Levam mark + "LEVAMCORP" + "DORAL · FL"), a "CATEGORY / CLASE" row with a category-colored swatch dot and "0X OF 06", an icon + large title, a description paragraph, a 2×2 dashed metadata grid (CLASS/CODE/ORIGIN/SHIPPING), and a footer on slightly darker cream (`#efece4`) with a decorative barcode strip and a `TAG·XXXX·CODE` + `levamcorp.com` line. Six categories (`CATEGORY_LABELS`): Televisions, Electronics, Kitchen Appliances, Gaming, Audio, Computers & Accessories — this replaced the previous four-category set (Televisions/Electronics/Small Appliances/Kitchen & Cooking) entirely, per the mockup's own content, not a preserved-copy pass. Typography is JetBrains Mono (labels/codes) + Archivo (title/description) — a deliberate one-off swap from the site's Space Grotesk/Inter pairing, matching what the physical label medium would actually be printed in. Don't reuse this cream-label treatment elsewhere without the user asking — it's specific to categories, not a new sitewide alternative to `Card`.
  Each label is wrapped in `TiltCard` (`glow={item.swatch}`, per-category color) for a cursor-following 3D perspective tilt on hover, and the label's own border switches from neutral `#dcd8ce` to that category's swatch color via a `--label-accent` CSS custom property plus a `.category-label:hover { border-color: var(--label-accent) !important; }` rule — `!important` is required there because the base border is set inline (React `style`), which otherwise always beats an external class rule regardless of specificity tricks. `TiltCard`'s glow-shadow color used to be a hardcoded ternary covering only 5 pre-existing site accents; it now calls the generic `hexToRgb()` helper so it works with any accent color, including these six category swatches.
- **PackingList** ("Built for serious business," `src/app/page.js`, `id="features"`): from a fourth Claude Design handoff, replaces a 3×2 grid of `TiltCard`/`Card` tiles with a single 6-line packing-list table — same monochrome scan-ticket language as `ManifestMetrics`/`ProcessStepper` above. Each line (Wholesale pricing, Verified partners only, 48h dispatch average, Live catalog access, U.S. based operation, Dedicated support — same six facts and colors as the tiles they replaced, just re-cast as table rows) shows a number, title, and a right-aligned key/value/tag block; hovering or focusing a row lights its left accent bar, shifts its content right (`transform: translateX(14px)`, not padding — see note below), and expands its description via the same `grid-template-rows` technique as `ProcessStepper`'s metadata and the FAQ accordion. Clicking/tapping a row also sets it as the persistently "active" line (independent of the transient hover state) so touch devices — which have no real hover — get a working toggle; this mirrors `ProcessStepper`'s own hover-preview-vs-click-persisted-selection split. The footer barcode ruler fills toward the selected row's position (`(index+1)/6` of the strip) in that row's accent color, using the same seeded-bar generator as `ManifestMetrics`/`ProcessStepper` (`seededBars`, reused directly). A first draft animated `padding-left` for the same "shift right" effect the mockup itself uses; the anti-pattern detector flagged it as the same layout-thrashing pattern as the `max-height` case above, so it was rebuilt as a fixed-padding row with an inner `transform: translateX(...)` wrapper instead — visually identical, compositor-only.
- **ManifestMetrics + ProcessStepper** (the SKU/dispatch/brands strip and the "how to apply" section, `src/app/page.js`, both under `id="stats"`): imported from a third Claude Design handoff, replacing a plain 3-stat grid and a static numbered-step list with two connected "scan ticket" panels — same header-bar / tag-code-and-barcode-footer language as `Card`, just monochrome (cream `#F5F1E8`-on-charcoal, no accent-tinted wash) since this is data, not a category. A dashed connector line with a filled dot (`ManifestConnector`) links the two panels, matching the mockup instead of the old wave-SVG dividers (`Wave` was removed — it had no other callers). `ManifestMetrics` shows 500+ SKUs / 48h dispatch / **10+** premium brands (raised from the old "7+" — the brand marquee now carries 13 real logos, so 10+ is the accurate floor) as odometer-style rolling digits (`OdometerNumber`) that count up once, triggered by `IntersectionObserver`, plus a decorative "barcode ruler" strip with a looping scan-light sweep (`manifestScan` keyframe). `ProcessStepper` auto-advances through the 4 steps every 5s, pauses and jumps to whichever step is hovered/focused/clicked, supports arrow-key navigation (`role="tablist"`), and drives its own progress ruler by writing `width` directly to a ref every `requestAnimationFrame` tick instead of storing per-frame progress in React state — the mockup's own version re-rendered a 120-bar list every frame, which this avoids. The active step's metadata fields expand via `grid-template-rows: 0fr → 1fr` (the same technique used by the FAQ accordion), not `max-height` — a first draft used `max-height` and the anti-pattern detector correctly flagged it as the same layout-thrashing pattern already fixed once in this codebase. Both panels' muted-text grays (`#7C7A73`, `#8F8C85`, `#6F6D67`, etc.) are a deliberate one-off tonal ramp lifted from the mockup for this ticket's own multi-tier hierarchy (label / active / inactive / hover) — don't fold them into `text-muted` or reuse them outside these two components.
- **LanguageDeclaration + BulletinGrid** (the "English & Español" and "Stay ahead of the market" sections, `src/app/page.js`, right after About): from a sixth Claude Design handoff, replacing two separate sections with one continuous pair. `LanguageDeclaration` recasts the same bilingual-support copy as a literal carbon-copy service form — a cream (`#F2EFE6`) card sitting on the dark page (the CategoryLabel precedent's "printed stock on dark ground" contrast, reused here), with a punch-hole row, an EN/ES tab switcher, and two overlapping "sheets" (English in front on `#FFFDF7`, Spanish behind on the duller `#DED9CC`, or vice versa) that swap z-order/stamp on click — each sheet carries a rotated ink stamp ("VERIFIED COPY" / "COPIA VERIFICADA"), four numbered rows of the real support copy, and two signature-style lines (rep assigned, hours). `BulletinGrid` rebuilds the market-insights teaser as ticket-styled cards over the same real `insightItems` data (unchanged) — a large ghost page-number per card, and hovering a card fully inverts it (dark → cream, cream ink) rather than just changing a border, with the footer barcode and status line tracking whichever card is hovered, same mechanism as `ManifestMetrics`/`CompanyRecord`. Both reuse `seededBars` for their barcode strips and `insightItems` (defined near `ProductPreview`) rather than duplicating the three real bulletin entries.
- **CompanyRecord** (the "About" section, `src/app/page.js`, `id="about"`): from a fifth Claude Design handoff, replaces the old two-column layout (checkmark list + `Card` info box) with a single "certificate of record" ticket — same scan-ticket family as `ManifestMetrics`/`ProcessStepper`/`PackingList` above (header strip with a blinking status dot, hoverable field rows with a left accent bar and `translateX` shift, footer barcode ruler tied to the hovered field's position). Left column keeps the exact existing headline/copy/four-fact list (now numbered `01`–`04` ledger rows instead of checkmarks) and the two circular "seals" (`SealRing`, a rotating-dashed-ring badge replacing `StampSeal` — see above). Right column's five fields dropped the old "Brands" field (a hardcoded, already-stale brand list that didn't match the real `BrandMarquee`) for a "Since: Registered · DBA on file" field instead, per the mockup's own field set. New: a real warehouse photo (`/public/warehouse.jpg`, downsized from a 2.1MB PNG the user provided to a 220KB JPEG) in an "Exhibit A · Warehouse" frame with corner brackets and a "Ref · WH-01" tag — this section's only photographic content on the whole page. A first draft animated `padding-left` for the field-row hover shift, same layout-thrashing pattern the detector had already caught twice on this page; fixed the same way, with a fixed-padding row plus an inner `transform: translateX(...)` wrapper.
- **CatalogSheet + CatalogCell** (the gated product preview, `src/app/page.js`, replaces the old `ProductPreview` — now deleted, no other callers) **and FoundersRecord** (the "Built by people who know the business" section, replaces the old inline two-`TiltCard` grid): from a seventh Claude Design handoff (two files, one project: `Levamcorp Catalog.dc.html` and a standalone `Levamcorp Founders.dc.html` with an identical founders section), restyled as a photo contact sheet and a "Signatories" ledger respectively, both in the same scan-ticket family as the rest of the page. `CatalogSheet` deliberately keeps fetching real data from `/api/public-products` (top-picks-first, ≤8 products, no price/sku) rather than the mockup's hardcoded demo SKUs — same "keep live data, restyle only" rule already applied to `BulletinGrid`'s `insightItems`. Each `CatalogCell` is a cursor-tracked 3D-tilt card (`transformStyle:'preserve-3d'`, `rotateX`/`rotateY` driven by pointer position, `translateZ` lift on hover) that fully inverts cream↔charcoal on hover/focus and fills its "Apply to see pricing" row with the accent color; a product with no `image_url` shows a "Drop {brand} photo" text placeholder, mirroring the mockup's own empty-state convention rather than a real broken-image icon. `FoundersRecord` uses the founders' initials ("VM"/"LE") inside the cream 3:4 photo-slot in place of a real photo, matching how the current site already had no real founder photos (previously circular initials avatars); bios, quotes, and contact info are unchanged from both the current site and the mockup, which carried identical copy. Verified locally against a temporary stubbed `/api/public-products` response (never committed) since this sandbox has no Supabase credentials to hit the real endpoint.
- **ManifestBackdrop** — **currently unmounted** (`src/app/page.js`, the component is still defined but its `<ManifestBackdrop/>` call under "FIXED DARK BASE" is commented out; the page runs on the plain `#000000` base div alone) per explicit request to go back to a flat background while a preferred backdrop design is worked out — don't delete the component or re-enable it without being asked. When it was mounted (sitewide fixed background, `src/app/page.js`, mounted once at the page root): from an eighth Claude Design handoff, replaces the old flat `repeating-linear-gradient` grid-texture layer with an animated 3D "cargo tunnel" — a perspective floor grid and ceiling grid drifting past each other, seven small paper "manifest tags" (SKU/ETA/origin/etc., each with a mini barcode) drifting in 3D depth with a gentle mouse-parallax tilt, an accent glow, and a vignette back to the page's base (`#000000` now; `#14120E` at the time this was built). Ported from the mockup's `DCLogic`/`renderVals()` per-frame re-render into a functional component that mutates transforms directly on refs inside a single `requestAnimationFrame` loop (mouse position eased at `0.06`/frame) rather than calling `setState` every frame — same "don't re-render the whole tree every tick" rule already applied to `ProcessStepper`'s ruler. Per-tag opacity/blur depend only on its fixed depth (`z`), so those are computed once at render instead of every frame; only `transform` is written imperatively. The mockup's own default accent (`#ff7a45` orange) was swapped for the yellow this doc committed to at the time (`#F2B705`) — see the Colors section above for the two swaps since (yellow → the mockup's own orange → Levam's brand blue, `#2F7DF6`, which is this component's accent now). **Deliberately excluded from the hero**: the hero `<section>` was given an explicit opaque `background:'#000000'` (previously relied entirely on the video/scrim to appear opaque) so the backdrop stays hidden behind it — this also fixed a latent bug where a failed/unloaded hero video (a known, documented risk — see `HeroVideoBackground` above) would have let the backdrop bleed through the hero, which the old, much subtler flat-grid texture never made visible. On the first pass the tags' default opacity (`0.9 − far×0.55`, i.e. ~0.35–0.78) read as too prominent, sometimes landing directly over foreground text as the page scrolled past the viewport-fixed tags; per explicit feedback ("no quiero que las etiquetas tomen tanto protagonismo") the whole curve was scaled down by `×0.4` (~0.14–0.31) while keeping every tag's position, motion, and content exactly as designed — do not reposition or hide individual tags to dodge text, the fix is opacity only.
- **FAQRecord** (the FAQ section, `src/app/page.js`, `id="faq"`, replaces the old `FAQSection` name and its rounded-card accordion): from a ninth Claude Design handoff, recasts the FAQ as a "Query log" ticket — same header-strip/headline/barcode-footer family as the rest of the page, `maxWidth:1180` to match. Each question is a full-width row (`FAQRow`) with a number, a left accent band, and a right-aligned topic tag (`ELIG`/`COST`/`TIME`/`MOQ`/`SHIP`/`LANG`) plus a 13px +/− icon (a fixed horizontal bar and a vertical bar whose opacity toggles 1→0); the open answer also shows 1–2 key/value "fields" (e.g. `Turnaround · 1–2 days`) that weren't in the old accordion. The six Q&As themselves were **updated to the mockup's own more detailed answers** (e.g. MOQ now states the actual `1–100 units` range and that the portal shows it per-SKU) rather than kept byte-identical — this one departs from the usual "style-only" rule because the mockup visibly authored new, more specific copy, the same call made for `CategoryLabel` earlier. Expand/collapse uses the same `grid-template-rows: 0fr → 1fr` technique as the old accordion and `ProcessStepper` (the mockup's own version animates `max-height`, already known to be wrong for this codebase). The mockup's per-row `padding-left`/`padding-right` inset on open was dropped rather than ported — animating padding here is the same layout-thrashing anti-pattern already fixed three times on this page, and unlike those cases there was no simple `translateX` substitute that matched the mockup's grid-column layout, so the row now signals "open" with just the background tint and accent band it already had. Below the list, a two-panel CTA block new to this section: an accent-colored "Boarding pass" (large `5 min to apply` / `48 hrs to answer` stats, a field grid, a barcode footer) and a cream "Passenger copy" (a 3-item checklist plus the existing Apply/Contact buttons, styled as CSS-class hover swaps — `.faq-cta-primary`/`.faq-cta-secondary` — rather than inline JS state, since it's a plain two-color swap). **Note:** this duplicates the separate `id="cta"` section further down the page ("Apply in 5 minutes. Response in 48 hours." + the same two buttons) almost exactly in message — that section wasn't part of this handoff so it was left alone, but the two now sit only one section apart.
- **DispatchDesk** (Contact + Footer, `src/app/page.js`, `id="contact"`, replaces the old 3-card `TiltCard` contact grid, the `FooterAsk` widget — now deleted, no other callers — and the old plain-text footer): from a tenth Claude Design handoff, a single component (its own hover state — which contact channel is under the cursor — drives a decoration all the way down in the footer, so it's one function rather than two) covering three stacked tickets. **"Dispatch desk"**: a 4-cell channel grid (Email/Phone/WhatsApp/Warehouse, `ChannelCell`) that inverts cream↔charcoal on hover like `CatalogCell`; kept the site's real two-line phone number (mockup's own example showed only one) and added a Google Maps link on the Warehouse cell the mockup didn't have. **"Enquiry slip"**: a real working form (Company/Email/Interest-dropdown/Message) wired to the existing `/api/send-contact-email` endpoint — since that endpoint takes `{name,email,company,phone,message}` and this form has no name/phone field, `company` doubles as the name fallback and the interest dropdown's value is prefixed onto the message body rather than extending the API. A barcode strip fills left-to-right as the three text fields get filled in (`seededBars(88117,70)`, reused directly). The **footer** rebuild adds three real link columns (Catalog/Company/Partners) — hrefs were corrected to the site's actual anchors/routes where the mockup used placeholder ones (e.g. `#products`/`#brands` both actually exist as `#brands` on this page; `/insights`, `/portal`, `/apply`, `/terms`, `/privacy` are real routes; "Shipping & returns" points to the existing `/rma` — Return Merchandise Authorization — page, the closest real match) — plus a second barcode ruler (`seededBars(99137,120)`) that lights up a quarter of its length per hovered contact channel above, echoing the mockup's own cross-component hover link.
- **Portal sign-in** (`src/app/portal/page.js`): **rebuilt a second time** from a dedicated Claude Design handoff (`Levamcorp_Portal_Login.dc.html`), replacing the freehand two-column version below. **This page is now a deliberate, explicit exception to the "portal ground is white" rule above** — it's the one portal screen that runs on the homepage's dark `#08090B` ground with the drifting-3D-tag backdrop (see `ManifestBackdrop` below for the general technique), because the user specifically designed and asked for this look for the sign-in gate; every page *behind* the gate (dashboard onward) stays white per the existing rule. Layout: a full-bleed dark page with a mouse-parallax perspective floor grid and four floating "manifest tag" cards (ported the same way `ManifestBackdrop` was — a `requestAnimationFrame` loop isolated in its own `LoginBackdrop` child component so the ~60fps animation state doesn't re-render the form on every frame); a top bar (logo + "Access point · Form 09") and a headline sit directly on that background, above a two-panel card: a cream "Sign-in slip" (email/password, forgot-password, submit) now on the **left** and an accent-blue "Partner stub" (500+ SKUs, the four perks, a barcode-style bar chart that fills as the two fields validate) on the **right** — the mirror image of the original left-dark/right-form arrangement. All auth logic (`handleLogin`, `handleForgotPassword`, the admin-email redirect, the reset-password redirect URL) is byte-for-byte unchanged; only the "Keep me signed in" checkbox and the show/hide-password button are new UI wired to fresh local state (both were already present, just restyled). The mockup's own accent (`#2b7fff`) was swapped for the site's committed `#2F7DF6` per the usual rule, and its "Don't have access?" sub-line under the headline (not in the mockup) was dropped to match the mockup exactly rather than carried over from the old page. Verified with Playwright at desktop and phone widths: the dynamic clearance/status text and the stub's barcode fill all update correctly as the fields are typed, and a real (failing, no backend in this sandbox) login attempt surfaces the existing error box correctly inside the new design.
- **Portal sign-in, original freehand version** (superseded by the entry above, kept for history): first page of the client-portal migration into this same visual vocabulary, done freehand (no Claude Design handoff — the user asked for the homepage's established language carried over, not a new mockup) rather than from a file. Split two-column layout kept from the old "mission control" version (left brand/pitch panel, right form), but recast: page ground is white; the left panel is a dark (`#08090B`) ticket with the usual header strip (accent dot + "Access point · Form 09"), the four pitch bullets became numbered ledger rows (same `01`–`04` pattern as `CompanyRecord`), and a dim barcode ruler sits above the copyright line. The right panel is a white "Sign-in slip" (bordered, not cream — cream would barely read against a white page) using the bordered-grid labeled-input pattern from `DispatchDesk`'s Enquiry slip (Email/Password rows, JetBrains Mono uppercase labels) instead of the old rounded translucent inputs; the submit button fills with the accent color on hover via a `.portal-submit:hover` class, matching `.faq-cta-primary`. All auth logic (`handleLogin`, `handleForgotPassword`, admin-email redirect, password-visibility toggle) is untouched — this was a pure restyle.
- **Portal dashboard, original freehand version** (superseded by the entry below, kept for history): second page of the migration, also freehand. A shared `PortalNav` component (dark `#08090B` bar, active-tab underline via `usePathname`, square avatar-initial chip) replaces the old light-gray nav and will be reused verbatim on the remaining portal pages so the tab bar stays identical across all of them. The KPI grid, order table, and sidebar cards (account record, latest order, contact, perks) all became scan-ticket panels — cream `#F2EFE6` for data/read-only tickets, dark `#08090B` for the account-identity panels, both bordered per the note above. Order status badges kept their original semantic colors (blue/gold/steel/green/red) rather than being folded into the single site accent — these are functional state indicators, not decoration, matching how `Card`'s multi-color accent rotation is already treated as a separate, sanctioned exception to the "one accent" rule. **Caught during review:** the order-log ticket and the sidebar used `display:grid` with two columns and no `alignItems`, so CSS Grid's default `stretch` made the (often-shorter) order table's cream background stretch down to match the taller sidebar, leaving a large ugly blank cream block — invisible on the old page's low-contrast white-on-light-gray design, glaring on this one. Fixed with `alignItems:'start'` on the grid container. All data logic (the `orders`/`client`/`user` fetch, the redirect-if-unauthenticated and redirect-if-admin checks, `money`/`fmtDate` formatting, every computed stat) is byte-for-byte unchanged — restyle only, verified against a temporary local mock-data route (never committed) since this sandbox has no Supabase session to log in with.
- **Portal dashboard, rebuilt from a Claude Design mockup** (`src/app/portal/dashboard/page.js`): first page of a second, fuller portal redesign pass — the mockup (a single-canvas prototype covering Dashboard/Catalog/Invoices/Account as internal view-switches) was applied one real page at a time, starting here, per explicit request; the other three views are reference for future passes, not yet built. **Another documented exception to "portal ground is white"**, alongside the sign-in page: this page now runs on a warm parchment `#E4DFD2` ground with the same two-layer dot-grid texture as the sign-in backdrop, not `#FFFFFF`. Structure: a dark `#08090B` top nav (still the same 5 real routes — Dashboard/Catalog/Orders/Invoices/Payments — restyled to the mockup's JetBrains-Mono-tab-plus-avatar-chip look, not the mockup's own 4-tab internal-view nav, since those other pages are still separate routed pages); a hoverable 4-cell "Manifest · Account metrics" ticket with a barcode ruler that fills based on which cell is hovered; a 4-tile shortcut row; a two-column body (an "Order log" ticket with live status filter tabs — All/Open/Completed/Cancelled, counts computed from real order data — over a sidebar of a dark "Partner stub" card, a "Latest order" card, a "Your account team" contact card, and a "Partner benefits" list). **Every real number is still the same real computation as the original freehand version** (`totalOrders`, `totalSpent`, `pendingOrders`/`pendingValue`, `completedOrders`, `displayName`, `businessName`, `lastOrder` — verified via `git diff` filtered to logic-bearing lines) and the auth guard / admin-redirect / Supabase fetch block is untouched. **Where the mockup invented business concepts this codebase doesn't have, the real equivalent was kept instead of fabricating data**: the mockup's 4th KPI cell was "Your tier" (a volume-discount percentage tied to its own demo catalog's tier math, no such concept exists on `clients`/`orders`) — kept as "Completed" instead, the real 4th stat the old page already showed. The mockup's "Account balance" sidebar card assumed a `$25,000` revolving credit line — no such field or policy exists anywhere in this codebase, so it was **not** invented; the real "Latest order" card (order number/date/total/status/ETA, already real data) took that slot instead. The mockup's "Self-service" tool list (reorder/instant-quote/W-9-download/ship-to-edit/MOQ-exception, several as fake stateful toggles with no backend) was likewise dropped in favor of the already-real "Your account team" contact card and "Partner benefits" list. The mockup's per-order expandable stage/carrier/ship-to detail panel was **not** ported — the real `orders` table has no carrier or per-order ship-to field, and the already-migrated Orders page (`src/app/portal/orders/page.js`) already owns a real, data-backed version of that detail view — so each dashboard row now links out to `/portal/orders` instead of showing invented data. `accountRef` (e.g. `PRT · A1B2`) is a decorative reference code like the ones added to the transactional emails, but grounded in the real business-name initials + the real signed-in user's id, not random. **Real bug caught and fixed during this pass:** the new `PortalNav`'s link row and right-hand user block had no `flexWrap`, so at phone widths "Payments" got clipped off the edge of the screen with the page scrollable sideways — fixed by adding `flexWrap:'wrap'` throughout and switching the link row's fixed `height:60` to `minHeight:60` so wrapped links get vertical room instead of being cut off. Verified via Playwright (desktop hover/filter states, phone-width layout, the nav-wrap fix, zero console errors) against a temporary mock-data preview route (never committed).
- **Portal catalog, original freehand version + first follow-up filters** (superseded by the mockup rebuild entry below, kept for history): third page of the migration, the biggest and highest-risk one — this is the actual browse/cart/checkout/order-submission flow, including the exact `submitOrder` code path whose `product_sku` NOT-NULL bug was root-caused and fixed earlier in this project (see `CatalogSheet`/`FAQRecord`-adjacent history). Reuses the same `PortalNav` pattern from the dashboard. The product grid became a bordered, square-cornered ticket grid (1px dividers, corner brackets on each image, matching the homepage's `CatalogCell`) instead of individually rounded/shadowed cards; the quote drawer and checkout modal got the same dark-header/cream-ticket treatment as everywhere else. **Deliberately left alone:** the stock/MOQ colored boxes (green/amber/blue), the order-status-adjacent payment/shipping radio colors, and the Amazon (`#FF9900`)/Walmart (`#0071CE`) marketplace-link buttons — these are functional or third-party-brand colors, not decorative accent, same reasoning as the dashboard's order-status badges. Every state hook, `useEffect`, and handler (`addToCart`, `removeFromCart`, `submitOrder`, `downloadExcel`, the whole `ProductCard` component's stock/variation/prep-fee logic) is byte-for-byte identical to before — confirmed via `git diff` filtered to logic-bearing lines before shipping, not just eyeballed. Verified against a temporary local mock-data route (4 products covering top-pick, low-stock, out-of-stock-with-variations, and refurbished-condition cases; never committed): grid, hover, variation swatch selection (price/stock update correctly), add-to-quote, the quote drawer, and the full checkout modal (payment/shipping selection, the pickup-address/ship-to-address conditional blocks, the submit button's disabled state) all confirmed working. The actual Supabase insert can't be exercised in this sandbox (no credentials) — clicking submit correctly falls into the existing `catch` block and shows the existing generic error alert, which is the expected behavior here, not a regression. **Follow-up (functional, not visual):** added a brand filter (`<select>`, options derived from the live product list) and a "Hide out of stock" toggle alongside the category tabs, and both the Top Picks and All Products grids now always sort out-of-stock items (`stock === 0`) to the end of their group regardless of any filter — a plain stable `.sort()` on the already-filtered list, so in-stock ordering (by name, from the Supabase query) is preserved. The identical filter/sort pair was added to `src/app/admin/products/page.js` (admin's own product list, not part of the portal migration but the same underlying data) for consistency, since admins hit the same "which SKUs are actually sellable right now" problem. Verified against temporary mock-data preview routes on both pages (8 products, 3 out of stock, spanning 6 brands; never committed): brand dropdown narrows correctly, the toggle hides all zero-stock items, and out-of-stock items sink to the bottom of their section/list when shown.
- **Portal catalog, rebuilt from a Claude Design mockup** (`src/app/portal/catalog/page.js`): a second, fuller redesign pass, applied from a dedicated Claude Design handoff (`Levamcorp_Portal_Catalog.dc.html`), per explicit request to match it as closely as possible. Replaces the previous always-visible-add-to-cart card grid with the mockup's own interaction model: compact cards (tag row, square image, brand, name, price/MOQ, a "Specs · pricing · logistics" CTA bar) that open a full-screen **product-sheet modal** on click — a KPI grid (price/stock/MOQ/dispatch), a tabbed info panel (Specifications / Logistics / and a Prep center tab shown only when the product has a real `prep_fee`), a quantity stepper + live order total, "Add to quote", an "Ask your rep" WhatsApp link, prev/next navigation (click or arrow keys), Escape-to-close with a body-scroll lock, and a decorative barcode footer (`seededBars`, reimplemented locally per the established per-file convention). Category tabs gained a "Top picks" entry (merging the old always-pinned-on-top strip into the mockup's single-tab-list pattern, per the mockup's own design) while keeping the site's real 7-category list, not the mockup's shorter demo set; the brand filter and "hide out of stock" toggle from the prior follow-up (see above) were carried forward unchanged, as was the out-of-stock-sinks-to-the-bottom sort. **Real data kept, mockup inventions dropped:** the mockup's 3-tier "volume pricing" section (a fabricated 4.5%/9% quantity discount computed from arbitrary multipliers of `moq`) has no backing field on `products` — there is no tiered-price schema in this codebase — so it was not ported; every product still sells at its one real `price`. Its "MSRP" KPI and "% below MSRP" tag assume an `msrp` column that doesn't exist, dropped in favor of a real "Dispatch" KPI (the real `dispatch_days` field) as the fourth cell. Its Logistics tab's invented case-pack/freight-tier/incoterms/returns rows (no such fields in the schema) were dropped; only real fields are shown (`warehouse` — mapped to the real Doral, FL address already used at checkout when it's `WH: FL`, else shown as-is — `dispatch_days`, `moq`). The Specifications tab reuses real admin-entered fields (brand/category/condition/SKU/UPC/ASIN/weight/dimensions/description) in place of the mockup's invented generic `specs` array. The Prep center tab uses the real `prep_fee` field and the same per-unit math already used at checkout, and is hidden entirely for products with no prep fee rather than showing a fabricated one. Marketplace links (Amazon/Walmart) and the "Ask your rep" WhatsApp number (`wa.me/17864909005`) matched the site's own real values already used elsewhere, so those ported as-is. **Real addition the mockup doesn't have:** color/size variation swatches (from the real `variations` field) were added inside the modal, since the mockup's demo products have no variation concept but real Levam products do — selecting a variation updates price, stock, and image exactly as the previous card-level swatches did. **Real bug caught and fixed during this pass:** the shared `PortalNav` (this page's own copy, per the one-nav-per-page convention) had no `flexWrap` on its link row or right-button group and used a fixed `height:60`, so at phone widths the nav overflowed horizontally (390px viewport, ~910px scroll width) — the same class of bug already caught and fixed on the dashboard's copy of this nav. Fixed identically: `flexWrap:'wrap'` throughout, `minHeight:60` instead of fixed height. Verified via Playwright against a temporary mock-data preview route (never committed): grid render and stock-sort order, all three modal tabs, variation selection updating price/stock/prep-fee math live, quantity stepper, add-to-quote correctly populating the existing (untouched) quote drawer and its total, prev/next navigation resetting per-item state, Escape-to-close, the Top Picks tab, and a 390px-wide pass confirming the nav-overflow fix (scrollWidth now equals clientWidth) — zero console errors throughout. **Follow-up:** the "Top picks" tab alone (needing a click) turned out to be too easy to miss, so on the "All products" tab specifically, top-pick products are once again pinned in their own featured strip above an "All products" divider — restoring the original always-visible placement — while the "Top picks" tab still works standalone as a dedicated filter (no duplicate strip shown there). Card markup was factored into one `renderCard(product, i)` helper shared by both the strip and the regular grid, with `i` resolved via `filtered.indexOf(product)` so the product-sheet modal's index and prev/next order stay correct regardless of which section a card was opened from.
- **Portal orders, original freehand version** (superseded by the mockup rebuild entry below, kept for history): fourth page of the migration. The order list became bordered cream ticket rows (status badge, "upload docs needed" flag, and a progress-step dot rail all recolored to the site's semantic status colors — blue/kraft/steel/green/red, same reasoning as the dashboard); the detail panel got the dark-header/cream-body ticket treatment, including the BOL/shipping-label upload dropzones (still real file uploads to Supabase Storage, untouched). **Caught during this pass, not by the detector:** the order row's selected/needs-docs border used the `border` shorthand together with a separate `borderLeft` in the same style object, with the shared value changing on click (selection) — this is a real React DOM warning ("updating a style property during rerender... don't mix shorthand and non-shorthand properties"), not just a lint nitpick, because the two properties can desync on a partial re-render. Fixed by expanding to explicit `borderTop`/`borderRight`/`borderBottom` alongside `borderLeft`. The same shorthand+longhand pattern exists in several already-shipped places on this page and the homepage (the small `border` + `borderLeft` accent-dot icons) but those are all static hardcoded colors that never change after mount, so they never trigger the warning in practice — only a *dynamic* border value sharing an element with a longhand sibling is an actual bug; don't blanket-fix the static ones. `uploadDoc`, `handleLogout`, the `orders` fetch (including its `.or()` filter matching both `user_id` and a notes-email fallback), and `statusConfig`'s labels/steps/descriptions are all unchanged — only `color` values were edited and the unused `bg` field was dropped now that badges use tinted borders instead of filled backgrounds.
- **Portal orders, rebuilt from a Claude Design mockup** (`src/app/portal/orders/page.js`): a second, fuller redesign pass from a dedicated Claude Design handoff (`Levamcorp_Portal_Orders.dc.html`), per explicit request to match it closely — same family as the catalog rebuild above. Replaces the split list/sticky-detail-panel layout with the mockup's own structure: a 3-cell KPI row, status tabs, a dense horizontally-scrollable order table (`data-scroll` wrapper, `minWidth:1020` inner content — a deliberate exception to the rest of the portal's responsive-grid convention, matching how the mockup itself treats a dense operational table), and a full order-record modal (5-stage timeline, prev/next navigation, Escape-to-close) replacing the old inline sticky sidebar. **Real data kept, mockup inventions dropped or substituted:** the mockup's per-order `carrier`/`tracking number`/`Incoterms` fields don't exist anywhere in this schema (confirmed via a repo-wide grep — no `orders` column or admin UI for them), so the "Where it's going" panel was rebuilt as "Shipment details" using only real fields: `shipping_method` from the linked `payments` row (mapped through the same `SHIPPING_METHOD_LABELS` already used at catalog checkout), the constant real Doral, FL pickup address, and the real optional `shipment_weight`/`shipment_dimensions`/`shipment_pallets`/`shipment_notes` fields the existing page already showed. The mockup's "Download your documents" (fabricated auto-generated Invoice/Packing-list/Commercial-invoice PDFs — no such generated documents exist anywhere in this codebase) was replaced with the real, already-existing BOL/Shipping-Labels upload dropzones (still real Supabase Storage uploads, shown only when `status === 'confirmed'`, exactly as before). The mockup's inline "Payment status" panel (fabricated `Net 30`/`PAY-XXXX` reference fields) was rebuilt from a real `payments` row instead — status/method/proof-submitted, joined by `order_id` (a stricter join than the sibling invoices page's `user_id`-based one, so it also covers admin-created orders that have no `user_id`). The mockup's fabricated 3-tier fee formula (a hardcoded "$1/unit for Prep Center, $285 flat for Freight" invented calculation) was dropped for the real stored `order.total`/`order.subtotal`; a genuinely real "Delivery fee" line is parsed from the `Delivery fee: $X` substring the admin's manual-order feature already writes into `order.notes` (see the admin new-order entry above) — shown only when present, not fabricated for orders that don't have one. The mockup's local-state-only "Reorder" toggle (no real cart bridge exists between the orders and catalog pages) was replaced with a genuinely working "Reorder via WhatsApp" button that pre-fills a real WhatsApp message (`wa.me/17864909005`) listing the order's real items — same pre-fill pattern already used by `FloatingButtons.js` — rather than faking an add-to-cart action with nothing behind it. **Real addition the mockup doesn't have:** the nav's avatar-initials chip is wired to a real `clients` fetch (same `contact_name || email-prefix` pattern already established on the dashboard's `PortalNav`), not the mockup's static "PH". `PortalNav` was built with `flexWrap` from the start this time (the mobile-overflow bug already caught twice on other pages' copies of this nav). The KPI definitions are real and match established sitewide conventions exactly: "Orders in progress" and "Total you've spent" reuse the dashboard's own `pendingOrders`/`totalSpent` formulas verbatim; "You still owe" sums real `payments` rows with `status === 'requested'`, the same real definition `portal/payments/page.js` already uses for its own balance stat. Status tabs match the mockup's own 7-tab set exactly (All/Open/In review/Confirmed/Dispatched/Completed/Cancelled) rather than adding an 8th "Received" tab, per the "match it as closely as possible" request — `new`-status orders still surface correctly under All and Open. Verified via Playwright against a temporary mock-data preview route (never committed; 6 orders covering every status, a payments join, and a delivery-fee note): KPIs, tab filters and counts, search, the order-record modal for confirmed/cancelled/delivery-fee orders (timeline, shipment details, BOL/label upload, payment status, costs breakdown, reorder/invoice/rep links), prev/next navigation, and a 390px-wide pass confirming no nav overflow — zero console errors throughout.
- **Portal invoices, original freehand version** (superseded by the mockup rebuild entry below, kept for history): fifth page of the migration — the one that was already closest to this world's own aesthetic (it's a literal printable invoice document: dark header, QR code, items table, terms, signatures). **Load-bearing constraint specific to this page:** `handlePrint()` grabs `#invoice-print-area`'s raw `innerHTML` and writes it into a brand-new popup window with only a bare CSS reset — none of the page's own `<style>` block (with `.lc-mono`/`.lc-display`) travels with it. Every element inside `#invoice-print-area` therefore uses inline `fontFamily` (via local `MONO`/`DISPLAY` string constants) instead of `className="lc-mono"`/`"lc-display"`, so the printed/PDF'd invoice keeps the site's typography; only the nav, hero, and list outside the print area use the classes. Hero stats, the invoice list, and the invoice body itself all got the ticket treatment (cream/dark alternating panels, mono labels, the same progress-stepper pattern as `orders`). `handlePrint`, the `orders`+`payments` fetch, `getInvNum`'s synthesized invoice-number scheme, `getStep`/`getPaymentStatus`, and every total/balance calculation are all untouched. Verified against a temporary mock-data route (never committed) covering an unpaid invoice with a partial payment (balance-due bar, pending banner, Pay Now button) and a fully paid one (all four progress steps lit, paid banner, no disclaimer) — both render and calculate correctly.
- **Portal invoices, rebuilt from a Claude Design mockup** (`src/app/portal/invoices/page.js`): a second, fuller redesign pass from a dedicated Claude Design handoff (`Levamcorp_Portal_Invoices.dc.html`), per explicit request to change "the invoice designs" too, not just the browsing chrome. **The on-screen experience was replaced entirely** with the mockup's compact scan-ticket summary card (a dark header bar with the invoice number and status badge, a 4-cell meta grid, itemized lines, a totals breakdown, a total-due box, a contextual notice, a "How to pay this invoice" remittance panel, and Download PDF / See order / Ask your rep actions) in place of the old always-shown full letterhead document. **The full letterhead document did not go away** — it still exists byte-for-byte (dark header, QR code, date bar, 4-stage progress ribbon, From/Bill-to, items table, totals, preliminary-invoice disclaimer, terms, signature lines, footer) inside `#invoice-print-area`, now rendered `display:none` so it stays in the DOM purely for `handlePrint()`'s raw-`innerHTML` popup-and-print flow (unchanged) to still produce a proper business document — this is a deliberate exception to "restyle everything," since a letterhead/signature-block invoice is a real accounting artifact this app already relies on, not a decorative surface to swap out. Real KPIs (You still owe / Already paid / Total invoiced), new status tabs (All/Unpaid/Paid/Cancelled), and search are all new features added by this pass, built on the same 3-bucket invoice-status model (`completed`→Paid, `cancelled`→Cancelled, everything else→Unpaid) the mockup itself uses. **One real accounting-logic fix adopted from the mockup:** "Total invoiced" now excludes cancelled orders (previously it summed every order regardless of status) — cancelled invoices carry no real charge, so counting them as billed revenue was simply wrong; this KPI and its "N billed · N cancelled, not counted" sub-line match the mockup's own (correct) accounting rule. **Real data substituted for every mockup fabrication:** the mockup's "How to pay this invoice" panel invents a fictional bank (Truist, a fake account number, a fake Zelle address) — replaced with Levam's actual Bank of America remittance details (account/ACH routing/wire routing), the exact same numbers already sent in the real order-confirmation email (`admin/orders/page.js`'s `PAYMENT INFO` block) — this was also *added* to the previously-remittance-less full letterhead document as a genuine improvement, since the emailed confirmation already promises this info but the PDF invoice never carried it. The mockup's fixed per-invoice "Freight"/"Prep center" line items (fabricated dollar amounts with no backing field) were dropped for the same real Subtotal + parsed "Delivery fee" (from the `Delivery fee: $X` substring in `order.notes`, the identical parse already used on the orders page) + "Sales tax · resale exempt: $0.00" breakdown already established as honest on the orders page — no tax logic exists anywhere in this codebase, so $0.00 documents that fact rather than inventing one. The mockup's varying per-invoice "Terms" (Net 15/Net 30/Prepaid) was kept as the one real constant this system actually uses everywhere (Net 15); its specific "Paid on {date}" values were dropped for a plain "Paid in full" label, since no `paid_at`/`updated_at` timestamp exists on `orders` to source an exact date from. Preliminary/paid/cancelled notice copy reuses this page's own pre-existing, already-approved legal language (the "preliminary invoice… reserves the right to adjust pricing… processed only upon receipt and verification of full payment" disclaimer, the "payment received… nothing further is owed" confirmation) instead of the mockup's own wording (which invented an unsupported "5 business days" hold policy). `handlePrint`, the `orders` fetch, `getInvNum`'s synthesized invoice-number scheme, and every total/balance calculation inside the hidden full document are byte-for-byte unchanged. **Real bug caught and fixed during this pass:** the list/detail two-column grid used a hardcoded `minmax(300px,440px) minmax(0,1fr)` with no responsive fallback, so at phone widths (390px) the two columns were forced side-by-side and every cell's text wrapped one character per line — fixed with a `.inv-grid` class collapsing to a single column under an 860px media query (inline styles can't express `@media`, hence the added CSS class, following the same pattern as this file's existing `.lc-mono`/`.lc-display`/print-media rules). Verified via Playwright against a temporary mock-data preview route (never committed; 4 invoices covering unpaid-with-delivery-fee, paid, and cancelled): KPIs, tab filters, the summary card for all three states, the real remittance panel (shown only when unpaid), and the `handlePrint()` popup flow itself (captured via Playwright's popup-window event) all confirmed correct — the printed document renders with real data plus the newly-added payment-instructions block. A 390px-wide pass confirmed the grid-collapse fix (scrollWidth now equals clientWidth) — zero console errors throughout.
- **The printable invoice document itself** (`src/app/portal/invoices/page.js`'s `#invoice-print-area`, `src/app/admin/invoices/page.js`'s `printRef` block, and `src/app/admin/orders/page.js`'s `printInvoice()` raw-HTML template): replaced across all three real invoice-document generators from a dedicated Claude Design handoff (`Levam_Corp_Invoice_standalone.html`) — a "document"-mode bundled export (`<doc-page>`/`<sc-raw-table>`, pt-sized print layout), a different export format than every `.dc.html` interactive-canvas mockup used elsewhere in this doc; its template HTML was pulled out of the bundler's base64 asset manifest via a small extraction script rather than read directly. This **supersedes only the hidden full letterhead document** described in the Portal invoices entry above (dark header/QR code/4-stage progress ribbon) — that page's on-screen scan-ticket summary card, KPI row, status tabs, and search are untouched; only what `handlePrint()` actually prints changed. New document, all three files: a white page (no dark header), the real hosted logo, real address/phone (`(786) 878-4122`, standardized across all three — two of the three already used it, one used the sitewide WhatsApp number and was brought in line), an invoice title + number + a green/red/amber "Paid in full / Cancelled · void / Payment due" stamp, a 4-column meta grid, a Bill-to/Ship-to two-column block (client-facing and admin/orders only — manual invoices have no shipping concept, so that file is Bill-to only), an items table, a real-remittance panel shown only while unpaid, and a fuller Terms & Conditions/signature/footer block. **Real data kept, mockup fabrications rejected:** the mockup's own fictional "Truist Bank, N.A." remittance block was replaced with Levam's real Bank of America details (same numbers already used sitewide) on the two files that use that hardcoded constant; `admin/invoices/page.js` instead uses each manual invoice's own real `bank_name`/`account_name`/`account_number`/`routing_number`/`swift`/`bank_address` fields (a separate ad-hoc invoicing table with its own remittance data per invoice, not the shared constant). The mockup's fabricated ship-to ("Amazon FBA · MIA2", carrier "Estes Express") was dropped for the real `payments.shipping_method` (mapped through the existing `SHIPPING_METHOD_LABELS`) plus a ship-to address regex-parsed from the real `Shipping: {method} - {address}` substring already written into `payments.notes` — no carrier/tracking field exists anywhere in this schema, so neither is shown. The mockup's fixed Freight/Prep-center line items and its "pricing held for 5 business days" notice copy were dropped for this codebase's own already-established real Subtotal/Delivery-fee breakdown and honest notice language (see the Portal invoices entry above) — no such hold policy exists. `admin/orders/page.js`'s version keeps that file's own real features the other two don't have (the ETA banner, the partial-payment amount-paid/balance-due breakdown) and adds an "Order status" meta field using the file's existing real `STATUS_LABEL` map. **Two real pre-existing React bugs caught and fixed while testing this pass, same shorthand/longhand class already fixed twice elsewhere in this doc (Portal orders entry above):** the invoice-list row in `admin/invoices/page.js` and the order-list row in `admin/orders/page.js` both mixed the `border` shorthand with a separately-changing `borderLeft` — fixed identically by expanding to explicit `borderTop`/`borderRight`/`borderBottom` alongside `borderLeft`. Verified via Playwright against temporary mock-data preview routes on all three files (never committed) covering unpaid/paid/cancelled (and, for the client-facing and manual-invoice files, with/without remittance and notes) — correct data, correct conditional remittance visibility, and zero real console errors (the border warning gone) on every state.
- **Portal payments, original freehand version** (superseded by the mockup rebuild entry below, kept for history): sixth page of the migration. Hero balance stats, the per-order payment cards (order summary + payment-proof upload dropzone, still a real Supabase Storage upload via `uploadProof`), and the partial-payment balance bar all got the ticket treatment. **Real bug caught and fixed, same category as the one on the orders page:** the balance bar animated `width` directly (`transition: 'width 0.5s'`) — a layout-triggering property, flagged by the anti-pattern detector — carried over verbatim from the pre-existing code. Fixed by keeping the bar element at a fixed `width:100%` and driving the fill with `transform: scaleX(fraction)` + `transformOrigin:'left'` instead, animating `transform` only; verified the rendered fill percentage is pixel-identical before and after. `uploadProof`, the `payments`+`orders` fetch, and every balance/total calculation are untouched.
- **Portal payments, rebuilt from a Claude Design mockup** (`src/app/portal/payments/page.js`): a second, fuller redesign pass from a dedicated Claude Design handoff (`Levamcorp_Portal_Payments.dc.html`), replacing the freehand version below — same family as the catalog/orders/invoices rebuilds above. New structure: a hero with a "Due now" chip that becomes a "Pay now →"/"Proof under review"/"All caught up" CTA depending on real state, a 3-cell KPI row, an "Action required" panel (one two-step Send-payment/Upload-proof card per real `payments` row with `status === 'requested'`, plus a compact "proof submitted" row per `processing` one), a horizontally-scrollable payment-history table (`data-scroll`, `minWidth:820` — same dense-table convention as the orders rebuild) with All/Cleared/Under review tabs, and a payment-detail modal. **Real data kept, mockup inventions dropped or substituted:** the mockup's fictional Chase Bank wire/ACH details and its Zelle option were replaced with the same real Bank of America remittance block already used on invoices (`REMIT`, reused verbatim) — Zelle isn't a real payment method here at all (the real checkout only ever writes `payment_method` as `credit_card`/`debit_card`/`ach`/`wire`, confirmed via `portal/catalog/page.js`'s own payment-method list), so the method switcher is two real tabs, "Wire / ACH" and "Card"; since there's no real online card gateway, the Card tab honestly says so and links to WhatsApp instead of inventing a `levamcorp.com/pay` portal. The mockup's whole "Your terms" panel (Net 30 payment terms, a $25,000 credit limit, a 3% card surcharge) was fabricated credit-line policy with no backing field anywhere — the $25,000 credit line specifically was already rejected once before on the dashboard rebuild for the same reason — so it was rebuilt as a "Payment info" panel of only real facts: Net 15 (the one real sitewide term, same constant the invoices rebuild established), ACH fee None / Wire "Bank fees may apply" (matching the checkout's own real per-method copy), and Sales tax "Resale exempt" (the same honest $0 tax line already established on invoices). The mockup's payment-history rows invented a masked bank-account "Source" and a system-generated "Note" (e.g. "Reference matched automatically") that nothing in this schema produces — dropped. Its "Download receipt" action assumes a per-payment receipt document that doesn't exist — replaced with a real "View invoice →" link to `/portal/invoices` for cleared payments, and a real "View proof →" (opens the client's own uploaded `payment_proof_url` via a signed URL, same `createSignedUrl`-with-lowercase-bucket-fallback pattern already used in `admin/orders/page.js`'s `openDoc`) for ones under review. Its history "Applied to" invoice numbers now reuse the exact same synthesized `LC-{year}-{seq}` scheme from the invoices page (recomputed here from a parallel fetch of the client's full order list in the same sort order, mapped by `order_number`) instead of inventing a separate numbering scheme. **Real additions beyond the mockup:** the nav's "↓ Statement" button (decorative in the mockup) now generates a real printable statement — a `window.open`+`document.write` document listing every real payment (date/applied-to/method/amount/status), following the same print pattern already used for invoices; a "Copy details" button on the Wire/ACH panel copies the real remittance block via `navigator.clipboard`. The mockup's single hardcoded due-order-with-a-toggle became real `.map()`s over every `requested` and `processing` payment row, so multiple simultaneous due/pending orders render correctly, not just one. "Paid to date" is now computed directly from `payments` rows with `status === 'paid'` rather than the old page's separate "completed orders" query — a more accurate real number, same category of correction as the "exclude cancelled from total invoiced" fix on the invoices rebuild. **Real bug caught and fixed during this pass:** the payment-history table's `data-scroll`/`minWidth:820` wrapper sat directly inside the two-column page grid without `minWidth:0` on its grid-item parent, so CSS Grid's default `min-width:auto` let the table's intrinsic width stretch the whole page instead of scrolling inside its own container — invisible on desktop, a real ~450px horizontal overflow at 390px. Fixed by adding `minWidth:0` to the grid item, the standard fix for this exact CSS Grid overflow class (distinct from the `flexWrap`-missing bugs caught on this page's own nav copies elsewhere in this doc — this one is a grid child sizing bug, not a flex-wrap one). Verified via Playwright against a temporary mock-data preview route (never committed; payments spanning requested-with-partial-payment, requested-by-card, processing-with-proof, and two paid/cleared): KPIs, the due/review panels, method-tab switching, the copy-details button, history tab filters, the detail modal, and a 390px-wide pass confirming the overflow fix (scrollWidth now equals clientWidth) — zero real console errors throughout.
- **Portal reset-password** (`src/app/portal/reset-password/page.js`): seventh and final page of the portal migration — the client portal (`src/app/portal/**`) is now fully in this visual world, matching the note at the top of this section. Same two-column layout as the sign-in page (dark ticket pitch panel left, cream form right), reused almost verbatim since this is functionally the same "auth gate" pattern. All four states — verifying the recovery token, the password form once verified, the expired/invalid-link error, and the post-update success screen — got the same treatment. `verifyOtp`, `updateUser`, and all four `password`/`confirm`/`loading`/`done`/`error`/`ready` state transitions are untouched — pure restyle. Verified all four states render correctly against a temporary preview route (never committed) that read a `?state=` query param to force each branch, since the real flow requires a live Supabase recovery token this sandbox can't generate.

- **Apply page** (`src/app/apply/page.js`): rebuilt from a dedicated Claude Design mockup (`Levamcorp_Apply.dc.html`), replacing the old dark-blue-SaaS-card 3-step wizard with a scan-ticket-styled one — dark left "pitch" sidebar (headline, a step list with `DONE`/`NOW` status tags and a progress ruler) beside a cream `#F2EFE6` sheet holding the active step's fields, matching the sign-in/reset-password auth-gate layout language rather than inventing a new one. All real logic is untouched: the same `applications` insert shape (`ein: form.ein_number`, `ein_document_url`, `resale_tax_document_url`, `heard_about`, `heard_about_detail`), the same Supabase Storage upload paths (`ein/${Date.now()}-...`, `resale/${Date.now()}-...` in the `Documents` bucket), the same `/api/send-application-email` call, and the same per-step validation rules. **Real, load-bearing data kept verbatim, not simplified to the mockup's shorter demo lists:** the full 12-option `heard_about` enum (still a `<select>`, not chips, since the mockup's chip pattern doesn't scale to 12 options) — this field feeds `admin/marketing/page.js`'s `ORGANIC_CHANNEL_MAP`, so trimming it would silently break real analytics; the real dollar-tier `VOLUME_OPTIONS`; the real 8 `PRODUCT_CATEGORIES`; `YEARS_OPTIONS` kept as the real "1–2 years" wording, not the mockup's "1–3 years". **Genuine improvements adopted from the mockup, not just styling:** localStorage draft persistence (`lvm-apply-draft-v1`, restores in-progress `form`+`step` on reload, clears on successful submit); real client-side file validation on the upload dropzones (rejects non-PDF or >10MB with a shown error — the old page only set `accept=".pdf"`, which doesn't actually block a bad file); a "Not yet registered" business-type option with a matching "No LLC yet? Apply anyway" callout, replacing the old generic "Other" option; a fuller review step showing all 15 submitted fields instead of a partial 9-field summary. Verified directly against the real `/apply` route with Playwright (this page needs no auth, so no mock preview route was needed) — all three steps, real PDF uploads to both dropzones, the review screen, and the "submit without agreeing" validation state all confirmed correct; a final submit-with-agree attempt correctly reaches the real Supabase insert and fails gracefully with the existing generic error message, the same sandbox-credentials limitation as every other DB-write feature on this site.

## Do's and Don'ts

- Do keep product/business content (headlines, stats, FAQ answers, founder bios, contact info) byte-identical when doing future visual passes here — this redesign was explicitly style-only, not a content or functionality change.
- Do reuse the warehouse motif (kraft, the brand-blue accent, hazard stripes, stamps, the 3D box) for new marketing-site surfaces before inventing a new one.
- Don't add a kicker/eyebrow label above a heading (small-caps tag + hairline + heading) — this page's small-caps "Label" component was removed sitewide on this page for reading as a generic AI-template tell; headings stand alone now.
- Don't reintroduce ambient blue/purple radial-glow orbs as the dominant background treatment — that reads as the generic near-black-plus-neon-accent look this redesign was explicitly built to avoid.
- Don't port this world onto the admin panel or portal without a deliberate decision — those currently run a different, still-valid dark-blue system; mixing both without intent will look like two products stitched together.
- The 8 hero video clips are hosted on a personal CloudFront URL (`d8j0ntlcm91z4.cloudfront.net/user_.../`) generated via a third-party tool, not stored in this repo or any Levam-owned bucket — if that URL ever goes away the hero silently shows a black background (text and layout are unaffected, per HeroVideoBackground above, but the footage itself won't play). Re-host to Supabase Storage or a Levam-owned CDN before this is load-bearing for real traffic.
- Don't scroll-couple the hero video again (see HeroVideoBackground above) — it is one normal `100vh` section, video behind, content in front, page scrolls at normal speed. This was tried and reverted twice.
